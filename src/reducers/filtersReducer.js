const initialState = {
    filterLabel: "",
    filterText: ""
};

const filtersReducer = (state=initialState, action) => {
   switch (action.type) {
       default:
           return state;
   }
};

export default filtersReducer;